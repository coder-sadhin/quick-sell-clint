import React from 'react';
import { useTitle } from 'react-use';

const Blogs = () => {
    useTitle('Quick SELL- Blogs')
    return (
        <div>
            this is blogs
        </div>
    );
};

export default Blogs;